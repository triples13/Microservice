demo:
  security:
    jwt:
      tokenExpirationTime: 540 # minutes
      refreshTokenExpTime: 1080 # minutes
      tokenIssuer: EOUAAServer
      tokenSigningKey: xm8EV6Hy5RMFK4EEACIDAwQus

server:
  port: 9800
security:
  oauth2:
    resource:
      filter-order: 3

spring:
  application:
    name: ruleandalarmapi
  datasource:
    url: jdbc:postgresql://mlsolutionsuitepostgresdev.cohofwga1qua.us-east-1.rds.amazonaws.com:5432/mlsolutionsuitedev?currentSchema=public&user=mluser&password=microlanduserdb
    username: mluser
    password: microlanduserdb
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
        ddl-auto: update
    properties:
      temp:
          use_jdbc_metadata_defaults: false
      hibernate:
        jdbc:
          lob:
            non_contextual:
              creation: true
eureka:
  instance:
    prefer-ip-address: true
    metadata-map:
      user.name: springbootadmin
      user.password: admin@springboot
  client:
    healthcheck:
      enabled: true  
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
     defaultZone: http://eureka-server:8761/eureka/
    

management:
  security:
   enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
          
    jmx:
      exposure:
        include: "*"